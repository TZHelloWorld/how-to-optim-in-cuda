// ===========================================================================
// cuda_hello.h — CUDA kernel 启动函数的 C++ 接口声明
//
// 该头文件是 CUDA 层（cuda_hello.cu，由 nvcc 编译）与绑定层
// （pybind_wrapper.cpp，由 g++ 编译）之间的唯一契约：
// 绑定层只依赖此处声明的普通 C++ 函数，不感知任何 CUDA 语法。
// ===========================================================================
#pragma once

// 启动 CUDA kernel：以 grid(2,2) x block(4,4) 配置在 GPU 上打印各线程坐标。
// 内部完成 kernel launch、错误检查与设备同步，对调用方表现为同步函数。
void launch_cuda_hello();
